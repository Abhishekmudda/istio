apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: grafana-istio-destinationrule
  namespace: istio-system
spec:
  host: grafana
  trafficPolicy:
    tls:
      mode: DISABLE
