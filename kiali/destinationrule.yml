apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: kiali-istio-destinationrule
  namespace: istio-system
spec:
  host: kiali.istio-system.svc.cluster.local
  trafficPolicy:
    tls:
      mode: DISABLE
